type User {
  user_id: String!
  email: String
  displayName: String
}

type Query {
  getMyProfile: User
}

type Mutation {
  updateUser(email: String, displayName: String): User
}

schema {
  query: Query
  mutation: Mutation
}
